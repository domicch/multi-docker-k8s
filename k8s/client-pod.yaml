apiVersion: v1
kind: Pod
metadata:
    name: client-pod
    labels:
        component: web
spec:
    containers:
        - name: client
        image: domicch/complex_react_demo_client
        ports:
            - containPort: 3000
